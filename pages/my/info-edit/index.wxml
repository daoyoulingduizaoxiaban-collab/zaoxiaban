<nav nav-type="my" titleText="账号资料" />

<view wx:if="{{!canEdit}}" class="info-edit info-edit--empty">
  <view class="empty-title">{{disabledReason}}</view>
  <button wx:if="{{accessState === 'logged_out'}}" class="primary-action" bindtap="onLogin">去登录</button>
  <button wx:else class="primary-action" bindtap="onRefreshAccessState">重新检查状态</button>
</view>

<view wx:else class="info-edit">
  <view class="field-card">
    <view class="field-row">
      <text class="field-label">用户名</text>
      <input class="field-input" value="{{personInfo.name}}" placeholder="请输入用户名" bindinput="onNameChange" />
    </view>
    <view class="field-row">
      <text class="field-label">手机号</text>
      <input class="field-input" type="number" maxlength="11" value="{{personInfo.phone}}" placeholder="请输入 11 位手机号" bindinput="onPhoneChange" />
    </view>
    <view class="field-row field-row--block">
      <text class="field-label">性别</text>
      <radio-group class="gender-group" bindchange="onGenderChange">
        <label wx:for="{{genderOptions}}" wx:key="value" class="gender-option">
          <radio value="{{item.value}}" checked="{{personInfo.gender === item.value}}" />
          <text>{{item.label}}</text>
        </label>
      </radio-group>
    </view>
    <view class="field-row field-row--block">
      <text class="field-label">个人简介</text>
      <textarea class="field-textarea" value="{{personInfo.introduction}}" placeholder="请输入个人简介" maxlength="50" bindinput="onIntroductionChange" />
    </view>
  </view>
</view>

<view wx:if="{{canEdit}}" class="info-edit__save">
  <button class="primary-action" loading="{{isSubmitting}}" disabled="{{isSubmitting}}" bindtap="onSaveInfo">保存</button>
</view>
