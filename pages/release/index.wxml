<t-navbar title="开团入口" left-arrow fixed="{{true}}" placeholder="{{true}}" />

<view class="page">
  <view class="release-container">
    <view class="box" wx:if="{{canCreateGroupOrder}}">
      <t-cell title="新建团单" description="进入导游开团表单，团单资料会按账号状态保存" arrow bind:tap="goCreateGroupOrder" />
    </view>
    <view class="box" wx:else>
      <t-cell title="开团入口" description="{{accessText}}" />
    </view>
  </view>
</view>
