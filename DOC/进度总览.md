# 进度总览（白话版）

> 这份只保留**现状**，读得完就行。逐页发现 / 逐项修复的完整明细查 git log。功能标准在 **`BUSINESS_LOGIC_PRINCIPLES.md`**（业务文件）。

---

## 一句话现状

- ✅ **地基与迁移**：本地后端替代云开发、去 mock、登录即可用、可维护性重构 R0–R6——都完成。
- ✅ **全功能检视（30 页逐页）**：完成，明细查 git log。
- ✅ **修复（Phase0 + W1–W6）**：全部完成并经 Claude 逐项审核，明细查 git log。
- ⬜ **仅剩 #30**：设置页可视化 dataBackend/appEnv + isProd 硬关——**已定延后**到正式化阶段。
- 🔧 **用户 2026-07-28 报的一批**：多数已改并 commit（见 git log），**没修完的见下「待修复」**。
- 🧪 **自动化自测**：本次新增，见下。

---

## OWNER 已拍板的决策（2026-08-26，照做，不要再问）

> **本表是决策编号的唯一口径**，程式码注解与 commit 讯息引用的都是这套编号。当时的执行脉络见 `交接-执行脉络.md`（已从 repo 外搬进来）。
> 这份原本漏了第 1 条、整体前移一格，导致每个编号都比别处少 1，2026-08-27 已对齐。

| # | 决策 | 状态 |
| --- | --- | --- |
| 1 | **改完自己 commit + push**，不用问 | 持续 |
| 2 | 云环境先用 `cloud1-3gwlqssy1f1972a9`（与 DEV 同一个，之后才会另开）。**但程式架构要把环境 ID 挖空成可替换**，之后换环境不必动逻辑 | ✅ |
| 3 | 本机测试资料库加清理功能，**做成登入页上的按钮**给 OWNER 自己清 | ✅ |
| 4 | 删掉小程序内的假资料兜底（`repositories/*` 由 `allowSeedDataFallback` 控制那批死码）。**本地后端 `local-server/` 要保留**——OWNER 只有云开发免费版，靠它做测试，两者不是同一件事 | ✅ |
| 5 | 删讯息页；删个人资料目录页与其编辑页（功能已被「用户审核」取代） | ✅ |
| 6 | **没有商品库**（最终定案，问过太多次了）。**开团时同步新增商品，全站没有任何独立的商品管理页面。**商品不是独立实体，只是团单文件里的 `productList`。商品库页／商品浏览页／商品表单页／选品页／`ProductService`／`ProductRepository`／云端 `products` 资源与集合／商品库 tab，**全部整个删档** | ✅ |
| 7 | 主 tab 未登入**直接导登入页**，不要空态按钮；**登入后要导回原本要看的页** | ✅ |
| 8 | 子页面接上共用载入元件（`groupOrder/add`、`groupOrder/productList`；原本还有商品表单页与选品页，两者已删档） | ✅ |
| 9 | 删两个没人用的资料模型档（`models/Host.ts`、`models/Member.ts`） | ✅ |
| 10 | 开团时间防呆：**出团时间不得晚于收单截止**，前后端都补 | ✅ |
| 11 | 删掉的页面**整个删档**，不要只拿掉注册 | 持续 |
| 12 | 删三份冗余文件：`MVP_COMPLETION_CHECKLIST.md`、`DATA_LAYER_DECISION.md`（有效的边界规则已搬进 `DEVELOPMENT_GUIDE` §1.1）、`进度总览-明细归档.md` | ✅ |
| 13 | **不做库存／名额上限**（2026-08-27）。业务是**先收单、统计完才叫货**，团单与商品都不加 stock/quota，不做超卖防呆。要停收就手动切「停止收单」或等截止时间。**不要再提案加库存/限购/剩余名额** | ✅ 规格已写 |
| 14 | **收单截止后团主仍可代客下单**（2026-08-27，刻意保留的团主例外），**且要记录是帮谁下的**（文字：客户姓名+手机+代下单团主）。**不绑客户帐号、只有团主看得到**，不做认领 | ⬜ **未实作**，见下 |
| 15 | **不挡重复下单**（2026-08-27）。同客户同团单下多次就是**多笔独立订单**，各自走各自状态，不合并、不提示 | ✅ 现况即如此 |

**执行顺序与完整交接**见 `DOC/交接-执行脉络.md`。

> **决策 14 是新开发项，不是现况**。云端 `customerOrders.create` 目前把 `customerOpenId` 写成下单者自己，
> 且角色闸门要求下单者具 `customer` 角色（纯 `guide` 帐号不能下单）→ 代客下单现在**做不到**。
> **细则（2026-08-27 定案）**：代下单的订单**不绑客户帐号、只有团主看得到**。只以文字记客户姓名+手机，
> 加一个「代下单」标记与代下单团主；不比对手机号、不做待认领、不做事后认领。被代下单的人自己登入也看不到这笔。
> 规格见 `BUSINESS_LOGIC_PRINCIPLES.md` A6「代下单订单的归属」，开发项 **`C-PROXY-ORDER`**（Part C · C5），画面契约 `PAGE_MAP.md` §5。

---

## 待修复（用户 2026-07-28 报，未修完）

> 这几条是**还没修好或没修完**的。修好且你测过的就把那一行删掉；已 commit 的成堆改动不列这里（查 git log），避免变历史流水。

- **#C2 团单二维码（QR）**：已改为**进详情自动补生成 + 写回团单持久化**（`3bd7d80`，删手动按钮）；扫码 scene=shareToken 反查已接好。**待真机验证**（需 `groupOrderQr` + `businessData` 云函数都已部署）。
- **#E4 详情底部按钮**：已改单行三小按钮紧凑条，并删掉「复制入口」「客户下单入口」（`3bd7d80`）。**待真机验证**。
- **#F1 团单列表加删除 + 未付款防呆**：删除走软删、有客户未付款要提醒；动前先核对/补 `BUSINESS_LOGIC_PRINCIPLES.md`（未做）。
  **一并修**：`groupOrders.remove` 目前只写团单自己的 `deletedAt`，底下 `customerOrders` 原样留着 → **孤儿订单**。同一处改完。
- **#F4 用户审核加查询**：团主资料/用户审核是否同一份名单、名字显示不一致——待查两份名单来源（未做）。
- **#E2 价格区间三档只显示两档**：代码查无丢档处，**需你给「实际输入的三个数量」**才能定位。
- **#B1 云端弹旧「请输入取货/交付」**：代码已对（该句全仓已删），是云端跑旧版——**需你在开发者工具把 `businessData` 上传并部署**。
- ~~**#G1 未登录不导登录页**~~ ✅ 已修完（2026-08-26）：全站需登录的页都走 `requireLogin()`，未登录导登录页并带 `redirectTo` 回原页。`check-contract.js` 的 C3/C4 与冒烟会持续把关。

### 技术债 / 文件债（2026-08-27 只读稽核产出）

> 需求类的开发项不放这里，放 `BUSINESS_LOGIC_PRINCIPLES.md` Part C（目前唯一带 🚧 的是 `C-PROXY-ORDER`）。
> 这一段只装 **bug、技术债、文件债**。修好就删那一行。

- **🔴 申请团主会关掉申请人自己的客户功能**（2026-08-27 验证）：
  `cloudfunctions/businessData/resources/users.js:161-162` 的 `applyForRole` 把整个账户
  `status` 与 `reviewStatus` 都设成 `PENDING`；而 `lib/core.js:307` 的 `assertApprovedProfile`
  第一关就挡掉非 `APPROVED`（连角色都还没看）→ **客户一按「申请团主」，原本可用的看团单／下单全部被挡死**，
  要等管理员审完才恢复。违反 A3（`BUSINESS_LOGIC_PRINCIPLES.md:107,119`）「账户仍是 approved 客户，客户功能不受影响」。
  **OWNER 已拍板走方案 A**：照规格补 `guideApplication` 子结构，账户维持 approved。
  规格待 PM 补成 Part C 开发项（`guideApplication` 目前全仓库零实作，只存在文件里）。

- **🔴 列表硬上限 100 笔、无分页，且「先取 100 才过滤」**（2026-08-27 验证：`lib/core.js:360-363` 属实）：`cloudfunctions/businessData/lib/core.js` 的 `getAllActive`
  取回 100 笔**之后**才滤 `deletedAt` 与权限 → 软删累积后可见笔数低于 100 且**静默不报错**。
  影响 `customerOrders.listVisible`／`groupOrders`／`providers`／`users`；`operationLogs` 是 `limit(500)` 同病。
  改法：`deletedAt` 推进 `where`，加真分页。**验收**：seed 120 笔订单，`listVisible` 要全拿得到。
  顺带一起改 `customerOrders.js` 的 `listVisible` 逐笔 `await` 权限判定（100 笔＝100 次含 db 读的 async）。
- **🟠 `shareToken` 不是密码学随机**：`lib/core.js` 的 `Date.now().toString(36) + Math.random()...`，
  而它是客户进团单的唯一凭证。改 `crypto.randomBytes(16).toString('hex')`。
  ⚠️ **旧 token 必须继续有效**（已发出的分享链接与小程序码还在流通），只改新产生的路径，不做迁移。
- **闸门缺口**：`check-contract.js` 没有规则查「客户端／云端权限镜像」（`services/auth/roleScope.js` ↔ `cloudfunctions/businessData/lib/core.js`
  十几个同名函式），而 `DEVELOPMENT_GUIDE` §2 自己把它列为最重要的坑 → 加一条规则比对两边名单。
- **文件债**：① 本档「跑出的结果」约 40 行是历史流水账，只该留「现在几支测试、怎么跑、判定规则」，其余删；
  ② `BUSINESS_LOGIC_PRINCIPLES.md` 598 行把规格（A/B）与开发待办（C）／测试待办（D）混在一份，建议拆；
  ③ **Part C/D 的 checkbox 状态未校准**（部分做完没回来打勾），需逐项对 git log 核一次；
  ④ `PAGE_MAP.md` §4 与 `FIELD_DICT.md` §9 的「❓ 待拍板」从 08-25 挂到现在，需一个到期升级机制。

**要你处理**：#B1 部署 `businessData`；#C2 收尾后部署 `groupOrderQr`；#E2 提供你实际输入的三个数量。

---

## 二、自动化自测（2026-07-28 起；2026-08-25 换代到官方 wechatide 指令）

**目的**：不靠人点，脚本驱动开发者工具跑真实操作，红了就地修，让功能可回归验证。

> **2026-08-25 换代**：开发者工具改版后官方把自动化收进 `wechatide` 指令，已全数迁移，
> 不再用 `miniprogram-automator` + `auto-port` 接线。13 支测试原本各自复制一份助手，
> 现在共用 `local-server/lib/ide.js`。新拿到的能力：捞模拟器 console（失败自动印错误）、
> mock wx API（原生弹窗/相机可测）、云函数部署进 CLI。

### 现有测试
| 脚本 | 测什么 | 怎么判定 |
| --- | --- | --- |
| `local-server/check-contract.js` | **契约静态检查**（页面注册↔档案、元件注册、返回落点、分层铁律、存档跳转延迟、全仓 no-undef、文件 🚧 标记↔开发项） | 七条规则，规则出处与理由见 `DEVELOPMENT_GUIDE` §10。**不用 IDE，几秒跑完，收工前先跑这支** |
| `local-server/verify-actions.js` | **businessData 全动作**（6 资源，owner 身份跑一遍） | 自起 server(3998)+临时库；返回通用错误文案＝🚩(崩溃/漏 require)。**不用 IDE/不改配置，直接 `node` 跑** |
| `local-server/automator-smoke.js` | **全站 18 页**加载（app.json 主包+分包，不含登录页） | 见下方四条判定规则；接了 `page-state` 元件的判三态，没接的判自绘的 `pageErrorText`/`isPageLoading`/`accessDenied`；两者皆无才记 SKIP |
| `local-server/flow-test.js` | 报Bug 提交全流程（进页→填→提交） | 用唯一标记回查本地库，确认 UI 提交**真落库** |
| `local-server/flow-grouporder.js` | **团单开团**全流程（登录→进开团页→填表+加商品→提交） | 用唯一 title 回查 `groupOrders.listVisible`，确认**真落库** |
| `local-server/flow-review.js` | **用户审核**全流程（seed 待审用户→owner 进审核页→通过） | 回查该用户状态变 `approved` |
| `local-server/flow-order.js` | **客户下单**全流程（seed OPEN 团单→进下单页→填数量+姓名手机→提交） | 回查 `customerOrders.listByGroupOrder`，该团单下出现订单、金额>0 |
| `local-server/flow-payment.js` | **团主确认收款**全流程（seed PAID 订单→详情页弹窗→确认实收） | 回查订单状态变 `CONFIRMED(2)` |
| `local-server/flow-groupedit.js` | **团单编辑**全流程（seed 团单→add 页编辑模式预填→改标题→保存） | 回查 `groupOrders.getById` 标题变新值（走 update 路径） |
| `local-server/flow-provider.js` | **供应商新增**全流程（进 providers/edit→填名称+联系人→保存） | 回查 `providers.listVisible` 出现该供应商 |
| `local-server/flow-cancel.js` | **取消订单**全流程（seed UNPAID 订单→详情页取消弹窗→确认） | 回查订单状态变 `CANCELLED(3)` |
| `local-server/flow-profile.js` | **个人资料自编辑**全流程（进 my/info-edit→改昵称+手机→保存） | 回查 `users.getById(owner)` displayName 变新值 |
| `local-server/flow-delete.js` | **团单删除**全流程 | mock 原生确认弹窗回「确定」，走页面真实删除逻辑；回查团单不在列表 |
| `local-server/flow-image.js` | **选图上传**全流程（开团页内嵌新增商品：点上传→选图回调→写进 data→加入商品→开团落库） | mock `chooseMedia` 回合法图片；回查团单的 `productList` 里该商品带 3 张图，并验前端 3 张上限 |
| `local-server/flow-customer-share.js` | **真客户身份＋分享链接**三分支 | 切本机测试身份成纯客户；不带 token／带错 token 要被挡，带对 token 才进得去 |

### 怎么跑（连**已开**的开发者工具，勿重启）
1. 起本地后端：`node local-server/server.js`（改了云函数代码要**重启**它，它启动时才 require 一次）。
2. 切到本地后端：`node local-server/set-backend.js local`（**不改源码**，写的是本机储存，立即生效）。测完还原：`node local-server/set-backend.js cloud`。不带参数就是查现在是哪个。也可以在 App 里切：设置页最底下的「数据后端」（仅 DEV 显示）。
3. 开发者工具保持打开本专案；新版 `wechatide` 自动化不需要 `auto-port` 或 `WS_ENDPOINT`。
4. 跑：`node local-server/check-contract.js`；`node local-server/flow-test.js`；`npm run smoke`。

### 跑出的结果（冒烟为 2026-08-25 重跑，其余截至 2026-07-28）
- ✅ **verify-actions 全绿**（2026-08-26 重跑）：6 资源全部动作正常执行，零红旗——**后端功能层全通**。（`products` 资源已随商品库删档，不在其中。）
- ✅ **全站 18 页冒烟**（2026-08-26 重跑）：**PASS 18 / SKIP 0 / 问题 0**，每一页都验到了。（页数从 25 降到 18 是删页所致：讯息页、个人资料目录页与其编辑页、选品页、商品库三页。）分三层判：
  - 接了 `page-state` 元件、**或原始码有呼叫 `threeState(`** 的页 → 判三态（12 页）
  - 没接但有自绘载入旗标的页 → 判 `pageErrorText`／`isPageLoading`／`isLoading`／`accessDenied`（5 页）
  - 两者都没有的页 → **底线验证：真的渲染出节点**（8 页）。只证明画面有内容、onLoad 没炸，**不证明内容对不对**——这层抓得到的只有「页面没起来」，抓不到「停在自己的载入态」（例如搜索页的旗标叫 `isAccessLoading`，冒烟不认这名字）。
  - ⚠️ **导页与读资料必须走同一条通道**：两者是独立的指令呼叫，用不同通道问「现在在哪一页」会出现「它说在 A 页、读到的却是 B 页的 data」，整轮结论张冠李戴。所以读状态时一并回传路由并核对，不符就重读。2026-08-26 修掉这个之前，同一份程式码连跑两轮会得到 9 与 25 两种结果。
  - 换代当下冒烟一度报 13 个问题，逐一查证**全是判定方式的误判**，页面本身没坏。修正如下，之后照这几条判：
    ① 等页面**轮询到就绪**再判，不能固定睡几秒（睡法会读到权限检查还没跑完的状态）；
    ② **先登录再跑**——需登录的页面未登录时被闸门导去登录页是**正确行为**，不是失败；
    ③ 接了 `page-state` 元件的页判三态；**没接的改判它自绘的 `pageErrorText`／`isPageLoading`／`accessDenied`**，不能整页放过（否则 onLoad 炸掉、画面全白也照样算通过）。原本有几页挂了 `useAccessPage` 却没接元件，三态栏位永远在说谎，2026-08-26 已全数接上（决策 7）；
    ④ 判三态时只看 `pageState`，**别看 `isLoading`**——那是各页自用旗标，不驱动共用元件画面，有些页设完 ready 就没关它。
    另有两道防线：读不到页面状态要回报 ERROR（不能当成「这页没三态」而记 SKIP，否则连线一断就会整轮 SKIP、印「问题 0」然后算通过）；一页都没 PASS 时直接判失败。
  - 另修两个基建：CLI 回传改取「括号配平」那段（原本从第一个 `{` parse 到档尾，CLI 补印警告行就炸）；读页面状态改成一次执行取回精简物件（整包捞客户订单页 data 超过 60KB 会被截断）。
- ✅ 报Bug UI 提交 → 落库（marker 回查命中）。
- ✅ **团单开团 UI 全流程 → 落库**（owner 登录→开团页→填表+加商品→提交，回查 groupOrders 命中，商品数=1）。
- ✅ **用户审核 UI 全流程 → 生效**（seed 待审用户→owner 审核页→通过，回查状态变 approved）。
- ✅ **客户下单 UI 全流程 → 落库**（seed OPEN 团单带上架商品→进下单页→填数量算价 ¥20→提交，回查该团单下订单命中、金额>0）。**关键发现**：`CUSTOMER_ORDER_CREATE` 允许 owner，且 `getShareAccessError` 对 owner 直接放行 → **无需切 customer 身份/重编 IDE** 即可跑通下单 UI（原「待续」里"需切 customer 身份"的假设被推翻）。
- ✅ **团主确认收款 UI 全流程 → 生效**（seed 一张直接 PAID 的订单→owner 详情页开确认弹窗、预填实收=申报额→确认，回查订单状态变 CONFIRMED）。补全「下单→收款」资金闭环，且首次覆盖**弹窗式交互**（onConfirmPayment→handleDialogConfirm）。`create` 传付款方式+durable 凭证+申报额即 `hasInitialPayment` → 一次建成 PAID，省掉单独改状态。
- ✅ **团单编辑 UI 全流程 → 生效**（seed 团单→add 页编辑模式 `?id=` 预填→改标题→保存，回查标题变新值）。首次覆盖**编辑模式预填 + update 路径**（区别于 create）。`flow-groupedit.js` 的 `gotoPage` 把首个登录 reLaunch 也包进 try/catch 重试 → 启动瞬断消失、首跑即稳（后续流程照抄这版）。
- ✅ **供应商新增 UI 全流程 → 落库**（进 providers/edit→填名称+联系人→保存，回查供应商列表命中）。首次覆盖**独立目录资源（providers）**。`flow-provider.js` 引入 `dataWhenReady`（轮询到目标字段有定义再读）→ 根治「reLaunch 后拿到未就绪空句柄、data 字段全 undefined」的误读（之前靠重跑绕过，现在首跑即准）。**这是最稳的读法，后续流程都该用它读 isLoggedIn/门控字段。**
- ✅ **取消订单 UI 全流程 → 生效**（seed UNPAID 订单→owner 详情页开取消弹窗、填备注→确认，回查订单状态变 CANCELLED）。**订单状态机全覆盖**：创建(UNPAID/PAID)→确认收款(CONFIRMED)→取消(CANCELLED)。第二个弹窗式交互（onCancelOrder→handleCancelDialogConfirm）。
- ✅ **个人资料自编辑 UI 全流程 → 落库**（进 my/info-edit→改昵称+手机→保存，回查 owner displayName 变新值）。首次覆盖**用户自更新（users.save）**。注：info-edit 只改当前登录用户自己的档，会持久改 owner displayName（本地 test 库、各 flow 的 contactName 都显式传值，无副作用）。
- 🔧 **自测顺手修好 3 个真问题**：① local-server 是旧进程（不含 `feedbacks` 资源）→ 重启；② smoke `msg.type()` 当函数调致崩溃 → 兼容属性/函数并包 try/catch；③ smoke `mp.waitFor` 应为 `page.waitFor`（automator 版本漂移）。
- 📌 **画面栏位/操作流程合理性**：已由归档里的 30 页逐页检视覆盖并修复，本轮不重跑。

### UI 写流程测试要点（登录优先·必读，`flow-grouporder.js` 已踩通）
1. **必须先首次登录**：`refreshSession` 只刷新已有会话、**不建首次登录**。要先 `reLaunch('/pages/login/login')` → `callMethod('login')`（本地模式 `AuthService.login` 只用 `wx.login`+owner openId，**无需 getUserProfile 手势**），会话建成后持久化在 storage，后续页 refreshSession 才有得刷。登录页在分包、reLaunch 偶发超时 → 做 **best-effort（try/catch）**，会话建一次即可。
2. **分包页 reLaunch 后用 `mp.currentPage()` 重取页面**：直接用 reLaunch 返回的句柄可能是过期页（data 全 undefined、方法不存在）。
3. **填表走 `page.setData({formData, selectedGoods})` + `callMethod('onSave')`**：绕开 t-button 选择器和跨页 picker，专注证明写流程。
4. **避开内部文案正则**：商品字段禁词 `QA|mock|test|local|测试|本地|自动化…`（`lib/core.js` `INTERNAL_PRODUCT_COPY_RE`）；团单存商品快照、不校验，用捏造 goods 即可。
5. **首个 automator 调用偶发超时/`getCurrentPagesByDomain`/`page destroyed`**：IDE 预热/重编抖动，**重跑即可**（`flow-review.js` 常需跑第 2 次）。
6. **导页后必须轮询等页面就绪**（`lib/ide.js` 的 `dataWhenReady`，预设 10 次 × 800ms）。⚠️ **布林权限栏位要传严格就绪条件** `v => v === true`：`canSave`／`canEdit`／`isLoggedIn` 这类初始值就是 `false`，用预设判定会第一次轮询就拿到 `false` 直接当结论，等于完全没等鉴权跑完。
7. **回查断言别靠"是否在 pending 列表"**：`users.listPending` 会把刚审过的也带回来 → 改读该用户当前 `reviewStatus`（approved/pending_review）判定。
8. **reLaunch 后必须校验真的落对页、不中就重试**（`flow-order.js` 的 `gotoPage`）：任何被 devtools 监视的**源码改动（如刚切 `config.js`）触发热重载**，会把模拟器导航**重置到编译起始页**并悄悄覆盖你的 reLaunch → 你以为进了目标页、其实停在 my/index（`page.data()` 字段全 undefined 即此症）。对策：reLaunch 后查 `currentPage().path` 命中目标子串，不中重试最多 5 次。**下条流照抄。**

### 待续（下个 Claude 接手点）
- **已覆盖的 UI 写流程（12 个）**：报Bug、团单开团、用户审核、客户下单、团主确认收款、团单编辑、供应商新增、取消订单、个人资料自编辑、删除类、选图上传、客户分享路径。创建/更新/状态机(建→确认→取消)/审核/弹窗/管理列表/自更新各形态都有样板。**owner 路径能自动化的写入面已跑穷**（余下卡原生 showModal/chooseMedia、或需切身份/会话隔离，见下）。
- ~~删除类流程不可自动化~~ **2026-08-26 已解锁**：新版可 mock `wx.showModal` 直接回「确定」，回调照真实路径跑。见 `flow-delete.js`。
- **⚠ 团主新增（tourGuides）owner 路径别测**：`edit/index.ts` onSave 成功后 `AuthService.updateCurrentProfile(res.data)` 会把当前 owner 会话覆盖成新建的 guide 用户 → **污染 owner 会话、连累后续所有 flow**。要测得先切一个专门身份、且测完还原会话，非 owner 路径可做。
- **declarePaid（客户声明付款）owner 看不到**：按钮 UI 上 `isCustomer && status===0` 只给客户角色渲染 → 归「需切 customer 身份/GUI」桶。
- ~~真 customer 身份严格路径需 GUI 重编~~ **2026-08-26 已解锁**：本机测试身份存在储存里（`services/auth/localIdentity.js`），切完重新登录即生效，**不必重编**。见 `flow-customer-share.js`。
- ~~真图片上传不可自动化~~ **2026-08-26 部分解锁**：mock `chooseMedia` 后，「点上传→回调→写进 data→保存落库」整条 UI 路径可测（`flow-image.js`）。**仍未覆盖**：真实相机/相册选取，以及云存储上传本身（本机无云存储）。
- **新流程别再复制助手**：`gotoPage`（含重试）、`dataWhenReady`（轮询到就绪再读）、`bd`（打本地后端 seed/回查）全部收在 `local-server/lib/ide.js`，直接 require。
- ✅ smoke 已改成**先登录再跑**，并加了「预期错误」白名单（`detail` 裸开缺 ID 算 PASS）。

### ⚠️ 边界（务必认清）
- 自动化只证明**本地后端**这条路通。**云端**靠「地端云端双通」铁律的镜像代码保证——本地绿是强信号，不等于云端逐字相同。改数据层的云端动作必须同步改 `cloudfunctions/businessData`（否则正式环境报「资料操作不存在」）。
- ~~云函数部署是 GUI 动作，CLI 做不到~~ —— **2026-08-25 起不成立**：新版 `wechatide` 指令可直接部署云函数（`cloud_fn_deploy`），云端也能做「改→测→再改」的闭环了。

---

## 三、代码质量面板（微信开发者工具）

- ✅ **组件按需注入**：已加 `app.json` `lazyCodeLoading`（commit `22615db`）。
- ⏭ **图片/音频 >200K**：项目内无超标图片/音频（最大 76K），大文件仅 `resume/` 已在打包忽略清单——重新扫描即绿。
- ⏭ **无使用的组件**：全是 tdesign 库文件（`miniprogram_npm` 构建产物），且部分被在用组件内部依赖，硬删会连坐、且下次构建会回来——**不处理**，按需注入后运行时零开销。
- 说明：代码质量面板是**优化建议，不拦上传/审核/上架**。
