# 修复进度看板（给使用者看）

更新时间：2026-07-26

> **怎么看**：只有两种状态——**✅完成 / ⬜未完成**。**没有「进行中」「一半」**。一个项目只有在**我（Claude）亲自审核该项改动、确认对了**，才会翻成 ✅；agent 说做完但我还没验、或验出问题，一律是 ⬜。
>
> **规则**：① 检视阶段一行码没改，所有修复在此看板追踪。② **共用模组/地基我自己先做**（Phase 0），做完才派 agent，避免多人改同一文件、代码混乱。③ 每个 agent 只拿**互不重叠的页面文件**，不准碰 app.json、不准碰共用模组、不准自由发挥，只照对应「业务文件章节 + 进度总览发现」做。④ 对照标准：`BUSINESS_LOGIC_PRINCIPLES.md`、发现明细在 `进度总览.md`。

---

## Phase 0 — 共用模组 / 地基（Claude 自己做，做完才派 agent）

| 编号 | 项目 | 触及文件（共用） | 状态 |
| --- | --- | --- | --- |
| P0-1 | **权限/隐私收口**：非管理层的用户目录只回本人（listUsers/listGuides/listProviders）；团单详情服务端按归属决定回全量还是「团单+在售商品+自己那单」；选品取数只回上架且供应商有效 | `services/auth/roleScope.js`、`repositories/directoryRepository.js`、`services/customerOrder/*`、`repositories/groupOrderRepository.js`、`services/product/*`、`repositories/productRepository.js` | ⬜ |
| P0-2 | **废弃 4 孤儿页 + app.json + 我的页入口清理**：删 `pages/home`、`pages/release`、`pages/loginCode`、`pages/chat`、`pages/customer/edit`（并入 #18）；清 app.json 页面/分包注册；`pages/my/index` 九宫格移除 团单/客户订单/商品库/工作台/开团、设置列表移除普通用户的 个人资料/客户资料入口 | `app.json`、`pages/my/index.*`、删除上述页目录 | ⬜ |
| P0-3 | **三态基准**：确认共用 `components/page-state` 的 loading/error(重试)/empty 契约，产出「自绘三态→page-state」的改造范例（供 agent 参照 `pages/customerOrders/index`） | `components/page-state/*`（如需微调）、规范说明 | ⬜ |

---

## W1 — 团单开团流程（Agent A）｜文件域：`pages/groupOrder/`、`sub-pages/groupOrder/add/`

| 编号 | 项目（对应发现） | 状态 |
| --- | --- | --- |
| A-1 | #4 团单列表：删顶部重复「新建团单」按钮（留 FAB）、删冗余「查看详情」按钮、卡片加「人数+已收/应收」（客户不显金额）、合并两条取数路径 | ⬜ |
| A-2 | #5 新建团单：补必填校验（名称/时间/截止/取货/付款/联系人/电话/商品）、出团 vs 截止时间顺序校验、商品必填拦截、联系人/电话从 profile 预填、空商品提示文案仅空时显示 | ⬜ |

## W2 — 团单详情/商品/选品（Agent B）｜文件域：`sub-pages/groupOrder/detail`、`productList`、`product-picker`

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| B-1 | #6 详情：统计卡+全团客户订单+明细弹窗仅管理者显示；**客户视图新增「本团在售商品」内联展示（可收合）+ 只显自己那单**；error 加重试；订单卡显 customerName；删死码(onSave/goToOrderDetail/onBack)、修 data-memberId | ⬜ |
| B-2 | #7 本团商品：页面访问门控（非管理者挡下/导回）；error 重试；移除确认加「不影响历史订单」；删冗余 data-index | ⬜ |
| B-3 | #8 选品 picker：只列上架且供应商有效商品（接 P0-1 取数）；页面访问门控；error 重试；删死码(showLoading/hideLoading 对、calculateCount、龙井别名) | ⬜ |

## W3 — 商品库 + 客户订单小改（Agent C）｜文件域：`pages/productManagement`、`sub-pages/product/list`、`pages/customerOrders/`

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| C-1 | #12 商品库：onToggleStatus/onDelete 补 canManageProducts 前端二次校验；删除确认文案加「不影响历史团单与订单」 | ⬜ |
| C-2 | #14 商品浏览：error 三态接重试；与 #12「管理/浏览」定位对齐（命名/去重） | ⬜ |
| C-3 | #10 客户订单列表：`loadQaOrders` 等 QA 命名改名；删 `ROLE_TEXT` 的 provider 残留 | ⬜ |
| C-4 | #11 下单页：补「至少选一件商品」校验 | ⬜ |

## W4 — 个人资料归并 + 团主 + 供应商（Agent D）｜文件域：`pages/profile/`、`pages/tourGuides/`、`pages/providers/`

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| D-1 | #16/#17 profile/*：整页门控为**管理层专用**（接 P0-1 roleScope）；列表显示接 P0-1 收敛后的数据 | ⬜ |
| D-2 | #19 团主列表：三态补齐；显示接 P0-1（团主仅本人、管理层全量） | ⬜ |
| D-3 | #20 团主编辑：精简无意义「维护日期」字段；「编辑他人」归管理端、保留「申请团主」客户用途 | ⬜ |
| D-4 | #22 供应商列表：入口从「我的」移进**商品库内**（B5）；清 PROVIDER 残留角色分支；删 `getTabBar().setData({value:'providers'})` 死设置 | ⬜ |
| D-5 | #23 供应商编辑：精简「维护日期」；返回落点随入口调整 | ⬜ |

## W5 — 审核 / 记录 / 消息（Agent E）｜文件域：`pages/userReview`、`pages/operationLogs`、`pages/message`

| 编号 | 项目 | 状态 |
| --- | --- | --- |
| E-1 | #28 用户审核：拒绝/停用加**二次确认 + 必填原因**；角色到期日改**日期选择器** | ⬜ |
| E-2 | #27 操作记录：加时间筛选 + 分页 | ⬜ |
| E-3 | #25 消息：`openOrder` 删掉形同虚设的弹窗分支（统一跳订单页） | ⬜ |

---

## 待议（不派 agent，等你拍板补规格后再排）

| 项目 | 待你定什么 | 状态 |
| --- | --- | --- |
| #24 数据中心 | 指标给谁看/金额口径/团主范围，是否补业务文件专章 | ⬜ 待你拍板 |
| #29 搜索 | 是否补搜索专章（范围/口径） | ⬜ 待你拍板 |
| #30 设置 | 是否加 dataBackend/appEnv 可视化 + isProd 硬关（C7 正式化） | ⬜ 待你拍板 |
| 供应商停用/删除 | 停用/删除供应商实体「不破坏历史订单追溯」的口径（D-PROVIDER-ENTITY） | ⬜ 待你拍板 |

---

## 执行顺序

1. **Phase 0（Claude）**：P0-1 → P0-2 → P0-3，全绿后才派 agent。
2. **第一波并行**：W1(A)、W3(C)、W5(E)（文件域互不重叠、且不强依赖隐私收口）。
3. **第二波**：W2(B)、W4(D)（依赖 P0-1 的隐私/取数收口，放第二波确保地基已就绪）。
4. 每个 agent 回报后 **Claude 逐项审核**：对不上业务文件/发现、或碰了不该碰的文件 → 我自己改好再翻 ✅。
5. 全绿后合并 codex → main、推远端。
