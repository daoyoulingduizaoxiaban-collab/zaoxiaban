# QA BUG 單 - 待修問題清單

## 強制規則

1. **只能用真實 GUI 驗收。** QA 必須使用已開啟且可見的微信開發者工具右側模擬器畫面操作；不得用 CLI、腳本、API、automation launch、直接改頁面狀態或路由跳轉取代真人點擊、輸入、返回、切頁與畫面觀察。除非使用者明示，不得新開、重啟或切換到新的微信開發者工具視窗。

2. **微信開發者工具卡住時先自行恢復。** 若右側模擬器停在歡迎頁、空白頁、未重新編譯狀態，或畫面明顯不是目前專案可驗收狀態，QA 必須在同一個既有微信開發者工具視窗內點擊「編譯」，等待工具完成重新編譯，並確認右側模擬器真的載入目前專案畫面後再繼續驗收。這個恢復動作最多重試三次；三次後仍無法進入可驗收畫面，才停止工作並回報實際可見狀態與三次嘗試結果。不得因此新開視窗、重啟工具、改用 CLI 或跳過 GUI 驗收。

3. **BUG 單只存目前未通過的修正問題。** 本文件不得存開發需求、MVP gate、產品完成度、backlog、Priority、下一步、摘要、歷史清單或第二張表；這些內容應移到對應文件。Status 只能是 `不通過`，通過項目必須從本清單移除，不得保留 `通過` 狀態列。

4. **每個 BUG 必須小到能單獨修、單獨測、單獨關閉。** 不得保留籠統 BUG；若一列包含多頁、多角色、多流程、多個驗收點，或讓開發者看不出要修哪個具體行為，QA 必須立刻拆成可單元測試的小 BUG，移除或改寫原籠統列，並補上具體畫面、操作、預期結果與可能位置。若其實是開發需求，必須移到 `MVP_COMPLETION_CHECKLIST.md`。

5. **每次都要重新實測並即時更新文件。** 本文件不是歷史紀錄；不得因舊文字、舊通過紀錄或 agent 過去自稱測過就關閉項目。驗收時每完成一列，就必須立刻回到本文件更新該列結果與證據，再繼續下一列；不得等全部測完才一次更新。

6. **回報必須可核對，不得含糊。** 每次回報必須先給簡短結論，再給統計數量：本次範圍總數、已測數、通過數、不通過數、未完成或需介入數。每個 `不通過` 都要附一行簡短原因；任何需要使用者介入的項目必須單獨列出並說明要使用者授權或決定什麼。只要仍有未完成、未驗證或需使用者介入項目，就不得回報「全部完成」「全部修好」或等同結論。

目前共 22 筆有效待修或需使用者介入問題。

| 編號 | 功能範圍 | 問題 | 驗收標準 | 可能位置 | Status |
|---|---|---|---|---|---|
| QA-20260705-007 | 消息列表導航 | 在消息頁點擊未讀消息後，沒有打開消息詳情或標記閱讀，而是跳到 `pages/customerOrders/index` 客戶訂單 tab。 | 點擊消息列表項應進入與該消息對應的詳情或業務落地頁，並有清楚的已讀狀態更新；若跳轉到訂單，應定位到該訂單詳情而不是只打開列表。 | pages/message/index、消息跳轉邏輯 | 不通過 |
| QA-20260705-008 | 客戶訂單詳情入口 | 客戶訂單列表載入後，點擊第一筆訂單卡片與卡片文字區沒有打開訂單詳情；因此詳情版面、商品明細、付款備註與狀態記錄無法被 GUI 驗收。 | 點擊客戶訂單卡片應打開可掃描的訂單詳情，並呈現基本資料、商品明細、付款備註、狀態記錄；長內容不得擠成一段且不可被彈窗寬度壓縮到難讀。 | pages/customerOrders/index、訂單卡片點擊事件、訂單詳情彈窗 | 不通過 |
| QA-20260705-009 | 搜索入口渲染 | 從我的頁點「搜索」後，DevTools 页面路径先變成 `pages/search/index`，但模擬器畫面仍短暫停留在我的頁，路由與畫面不同步。 | 點擊搜索入口後，路由與畫面應同步切換；若需要載入，應在搜索頁顯示 loading，而不是保留來源頁內容。 | pages/my/index、pages/search/index、路由工具 | 不通過 |
| QA-20260705-010 | 搜索輸入框 | 搜索頁的搜索框在畫面與輔助樹中可見，但點擊與設定文字後仍停留在占位文字，未能進入可輸入狀態。 | 搜索框應可點擊聚焦、輸入關鍵字並即時或提交後顯示結果；取消與清空行為應正常。 | pages/search/index、input 綁定 | 不通過 |
| QA-20260705-014 | 開團表單預設內容 | 開團表單的多行欄位輔助樹中出現單字 `a` 作為內容或殘留文字，容易被視為測試資料。 | 新建團單表單初始狀態不得含測試字元；所有欄位應為空值或正式 placeholder，提交前必填校驗清楚。 | sub-pages/groupOrder/add/index、表單初始值 | 不通過 |
| QA-20260705-016 | 開發工具 Console 警告 | 2026-07-06 GUI 重測時，DevTools 頂部仍可見告警/計數顯示 `/132`，主流程操作後 warning 類問題未清零。 | 巡查主分頁、搜索、消息、商品、團單、客戶訂單、資料中心流程時，Console 不應新增 warning 或 error；必要警告應被修正或降噪。 | 全站前端流程、console warning 來源 | 不通過 |
| QA-20260705-019 | 個人資料頁返回 | 2026-07-06 GUI 複測時，從我的頁點「个人资料」可進入 `pages/profile/index`，但頁首返回按鈕點擊多次無反應，無法回到我的頁。 | 個人資料頁頁首返回應回到我的頁，返回後 DevTools 页面路径、可見畫面與底部 tab selected 狀態必須一致。 | pages/profile/index、navigation utility、custom navbar | 不通過 |
| QA-20260705-022 | 工作台商品庫快捷卡 | 2026-07-06 GUI 重測時，工作台「商品库」快捷卡可短暫打開 `pages/productManagement/index`，但路由、底部 tab selected 與可見畫面狀態互相覆蓋，後續又回到我的頁狀態，不能算穩定跳轉成功。 | 點擊工作台「商品库」快捷卡後，路由、底部 tab selected 與可見畫面必須一致停在商品庫；不得被我的頁 selected 狀態或其他 webview 覆蓋。 | pages/home/index、pages/productManagement/index、custom-tab-bar、navigation utility | 不通過 |
| QA-20260705-026 | 工作台授權狀態 | 從我的頁點「工作台」進入 `pages/home/index` 時，同一登入上下文先顯示「请先登录后继续使用」與「微信登录」，之後又在同一路由顯示「当前账号已通过微信登录，业务资料会同步保存。」 | 工作台應在授權判斷完成後再顯示內容；同一路由不得先顯示未登入阻擋再切回已登入業務資料。 | pages/home/index、auth service、role scope | 不通過 |
| QA-20260705-027 | 團單商品挑選搜尋 | 在 `sub-pages/groupOrder/product-picker/index` 點擊商品搜尋框並用鍵盤輸入 `Long` 後，輸入框仍顯示占位文字「搜索商品名称」，未進入可輸入狀態。 | 商品挑選頁搜尋框應可聚焦、輸入關鍵字並更新列表；輸入、清空與無結果狀態都應可見。 | sub-pages/groupOrder/product-picker/index、input 綁定 | 不通過 |
| QA-20260705-028 | 團單商品挑選選取 | 2026-07-06 GUI 重測時，商品挑選頁顯示「西湖龙井伴手礼礼盒」後，點擊左側選取圓點兩次，底部仍顯示「已选择 0 项」，「确认加入」維持 disabled。 | 點擊商品選取區或商品卡片後，已選數應更新為 1，確認加入應可用；再次點擊應可取消選取。 | sub-pages/groupOrder/product-picker/index、商品選取狀態 | 不通過 |
| QA-20260705-030 | 供應商申請入口 | 2026-07-06 GUI 重測時，我的頁已出現「申请供应商」，但點擊後仍停留在 `pages/my/index`，沒有進入 provider 申請流程。 | 使用者應可從正式 GUI 發起 provider 申請；申請後進入待審核狀態，owner/admin 可審核，通過後 provider 才能維護供應商資料與商品。 | pages/my/index、pages/providers/index、pages/providers/edit/index、services/auth/roleScope | 不通過 |
| QA-20260705-031 | 操作記錄入口 | 2026-07-06 GUI 重測時，我的頁可見「操作记录」，但點擊後仍停留在 `pages/my/index`，沒有進入操作記錄頁。 | 點擊「操作记录」應進入操作記錄頁，團主應能查看自己的團單、商品、收款相關操作記錄；owner/admin 應能查看自己的管理操作記錄，並支援列表、篩選、空狀態與權限隔離。 | pages/my/index、operationLogs repository/service、操作記錄頁 | 不通過 |
| QA-20260705-032 | 小程序品牌名稱 | 在工作台頁點開右上角小程序菜單後，菜單標題顯示「早点统计早下班」，與目前產品的團主/導遊領隊早下班定位及 DevTools 專案名稱不一致，使用者會以為進入了另一個小程序。 | 小程序菜單、導航標題、分享標題與正式產品名稱應一致；不得殘留舊產品或模板名稱。 | app.json、project.config.json、小程序帳號設定、分享配置 | 不通過 |
| QA-20260705-033 | 工作台「客户订单 2」統計卡片 | 2026-07-06 GUI 重測時，「进行中团单 3」可進團單列表，「待确认收款 0」可進客戶訂單且套用客戶付款篩選；但接著點「客户订单 2」後仍沿用前一次「客户付款」篩選/空狀態，沒有打開客戶訂單總數對應列表。 | 點擊「客户订单 2」應進入客戶訂單列表並顯示對應總數範圍，不得沿用前一次統計卡片的付款篩選。 | pages/home/index、pages/customerOrders/index、統計卡片跳轉參數與篩選重置 | 不通過 |
| QA-20260705-034 | 小程序預覽/上傳阻塞 | 此項驗證方式是 CLI `preview` / `upload`；目前強制規則禁止 CLI，因此 2026-07-06 未能重跑 CLI 驗證。舊紀錄顯示主包 2103KB 超過微信 2048KB 上限，需使用者明確授權 CLI 驗證或改由正式 GUI/工具清單接管。 | 使用者授權 CLI 驗證後，主包大小應低於 2048KB，且 `cli preview` / `cli upload` 能正常啟動；若不允許 CLI，此項應移出 GUI QA BUG 單，改由打包/工具檢查文件管理。 | `project.config.json`、資源白名單、`static` 大檔資源、`miniprogram_npm` | 不通過 |
| QA-20260705-035 | 人工 GUI 右側模擬被阻塞（二次驗證） | 此項依賴重新執行 `cli open / auto / preview`；目前強制規則禁止 CLI，因此 2026-07-06 未能重跑 CLI 二次驗證。需使用者明確授權 CLI，或決定此項移出 GUI QA BUG 單。 | 使用者授權 CLI 驗證後，`cli preview` 應成功進入模擬器；若不允許 CLI，此項不得由 GUI QA 假裝完成，應移到工具/打包驗證清單。 | 微信開發者工具 CLI、`cli preview`、`project.config.json`、`app.json` | 不通過 |
| QA-20260706-038 | 個人資料頁資料錯誤文案 | 2026-07-06 GUI 複測時，`pages/profile/index` 可打開，但頁面中央顯示「未知云端资料操作」，不是正式個人資料內容、正式空狀態或可理解的錯誤處理。 | 個人資料頁應顯示正式個人資料、正式空狀態或可操作的正式錯誤提示；不得顯示「未知云端资料操作」這類內部/不明錯誤文案。 | pages/profile/index、profile service、cloud data action mapping | 不通過 |
| QA-20260706-039 | 帳號資料頁返回 | 2026-07-06 GUI 複測時，從我的頁點「账号资料」可進入 `pages/my/info-edit/index`，但頁首返回按鈕點擊後仍停留在帳號資料頁，無法回到我的頁。 | 帳號資料頁頁首返回應回到我的頁，返回後 DevTools 页面路径、可見畫面與底部 tab selected 狀態必須一致。 | pages/my/info-edit/index、navigation utility、custom navbar | 不通過 |
| QA-20260706-040 | 帳號資料頁預設內容 | 2026-07-06 GUI 複測時，`pages/my/info-edit/index` 的「个人简介」多行欄位輔助樹出現殘留文字 `a`，屬於測試資料外露。 | 帳號資料頁初始狀態不得含測試字元；多行欄位應為空值或正式 placeholder。 | pages/my/info-edit/index、表單初始值 | 不通過 |
| QA-20260706-041 | 團主資料頁返回 | 2026-07-06 GUI 複測時，從我的頁點「团主资料」可進入 `pages/tourGuides/index`，但頁首返回按鈕點擊後仍停留在團主資料頁，無法回到我的頁。 | 團主資料頁頁首返回應回到我的頁，返回後 DevTools 页面路径、可見畫面與底部 tab selected 狀態必須一致。 | pages/tourGuides/index、navigation utility、custom navbar | 不通過 |
| QA-20260706-042 | 團主資料頁資料錯誤文案 | 2026-07-06 GUI 複測時，`pages/tourGuides/index` 可打開，但頁面顯示「未知云端资料操作」，不是正式團主資料內容、正式空狀態或可理解的錯誤處理。 | 團主資料頁應顯示正式團主資料、正式空狀態或可操作的正式錯誤提示；不得顯示「未知云端资料操作」這類內部/不明錯誤文案。 | pages/tourGuides/index、tour guide service、cloud data action mapping | 不通過 |
