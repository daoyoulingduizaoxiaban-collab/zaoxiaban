# QA BUG 單 - 待修問題清單

## 強制規則

1. **只能用真實 GUI 驗收。** QA 必須使用已開啟且可見的微信開發者工具右側模擬器畫面操作；不得用 CLI、腳本、API、automation launch、直接改頁面狀態或路由跳轉取代真人點擊、輸入、返回、切頁與畫面觀察。除非使用者明示，不得新開、重啟或切換到新的微信開發者工具視窗。

2. **BUG 成立證據只能來自右側模擬器真實畫面或可見操作結果。** 輔助樹、accessibility tree、DOM、工具讀到的 value、hidden text、不可見節點、console 內部資訊，只能用來輔助定位或找可能原因，不能單獨當作 QA BUG 證據。若右側模擬器肉眼看不到問題，且點擊、輸入、返回、切頁後也沒有可見錯誤結果，就不得報成 GUI BUG。所有 BUG 描述必須寫「畫面上看到什麼」或「實際操作後畫面怎麼失敗」，不得寫「輔助樹顯示什麼」當作問題本身。

3. **微信開發者工具卡住時先自行恢復。** 若右側模擬器停在歡迎頁、空白頁、未重新編譯狀態，或畫面明顯不是目前專案可驗收狀態，QA 必須在同一個既有微信開發者工具視窗內點擊「編譯」。每次點擊「編譯」後，至少等待 5 秒，並確認工具完成重新編譯、右側模擬器真的載入目前專案畫面後，才繼續驗收或判定本次恢復失敗。這個恢復動作最多重試三次；三次後仍無法進入可驗收畫面，才停止工作並回報實際可見狀態與三次嘗試結果。不得因此新開視窗、重啟工具、改用 CLI 或跳過 GUI 驗收。

4. **BUG 單只存目前未通過的修正問題。** 本文件不得存開發需求、MVP gate、產品完成度、backlog、Priority、下一步、摘要、歷史清單或第二張表；這些內容應移到對應文件。Status 只能是 `不通過`，通過項目必須從本清單移除，不得保留 `通過` 狀態列。

5. **每個 BUG 必須小到能單獨修、單獨測、單獨關閉。** 不得保留籠統 BUG；若一列包含多頁、多角色、多流程、多個驗收點，或讓開發者看不出要修哪個具體行為，QA 必須立刻拆成可單元測試的小 BUG，移除或改寫原籠統列，並補上具體畫面、操作、預期結果與可能位置。若其實是開發需求，必須移到 `MVP_COMPLETION_CHECKLIST.md`。

6. **每次都要重新實測並即時更新文件。** 本文件不是歷史紀錄；不得因舊文字、舊通過紀錄或 agent 過去自稱測過就關閉項目。驗收時每完成一列，就必須立刻回到本文件更新該列結果與證據，再繼續下一列；不得等全部測完才一次更新。

7. **回報必須可核對，不得含糊。** 每次回報必須先給簡短結論，再給統計數量：本次範圍總數、已測數、通過數、不通過數、未完成或需介入數。每個 `不通過` 都要附一行簡短原因；任何需要使用者介入的項目必須單獨列出並說明要使用者授權或決定什麼。只要仍有未完成、未驗證或需使用者介入項目，就不得回報「全部完成」「全部修好」或等同結論。

目前共 12 筆有效待修或需使用者介入問題。

| 編號 | 功能範圍 | 問題 | 驗收標準 | 可能位置 | Status |
|---|---|---|---|---|---|
| QA-20260705-007 | 消息訂單通知落地 | 2026-07-06 GUI 重測時，在消息頁點擊第一筆「已确认 云端客户｜团单｜￥24 已读」消息後，只跳到 `pages/customerOrders/index` 客戶訂單列表，沒有打開對應「云端客户｜￥24」訂單詳情。 | 點擊該消息後應直接定位到對應客戶訂單詳情或打開詳情彈層；不得只停在客戶訂單列表。 | pages/message/index、pages/customerOrders/index、消息跳轉參數、訂單詳情打開邏輯 | 不通過 |
| QA-20260705-010 | 搜索輸入框 | 2026-07-06 GUI 重測時，搜索頁點擊搜索框並輸入 `Long` 後，畫面上的搜索框仍停留在占位文字，沒有顯示輸入內容或搜尋結果。 | 搜索框應可點擊聚焦、輸入關鍵字並即時或提交後顯示結果；取消與清空行為應正常。 | pages/search/index、input 綁定 | 不通過 |
| QA-20260705-016 | 開發工具頂部 warning 計數未清零 | 2026-07-06 GUI 重測時，在已編譯並完成主要頁面切換後，DevTools 頂部仍顯示 warning/告警計數 `/132`。 | 進入專案並完成一次普通編譯後，DevTools 頂部不應持續顯示 warning/error 計數；若仍有計數，開發者需定位首條 warning 來源並修到不再出現。 | 全站前端初始化、DevTools Console warning 來源 | 不通過 |
| QA-20260705-019 | 個人資料頁返回 | 2026-07-06 GUI 複測時，從我的頁點「个人资料」可進入 `pages/profile/index`，但頁首返回按鈕點擊多次無反應，無法回到我的頁。 | 個人資料頁頁首返回應回到我的頁，返回後 DevTools 页面路径、可見畫面與底部 tab selected 狀態必須一致。 | pages/profile/index、navigation utility、custom navbar | 不通過 |
| QA-20260705-027 | 團單商品挑選搜尋輸入框 | 2026-07-06 GUI 重測時，在 `sub-pages/groupOrder/product-picker/index` 點擊商品搜尋框並輸入 `Long` 後，畫面上的商品搜尋框仍停在占位文字「搜索商品名称」，商品列表也沒有依關鍵字更新。 | 商品挑選頁搜尋框應可聚焦、輸入關鍵字並更新列表；輸入、清空與無結果狀態都應可見。 | sub-pages/groupOrder/product-picker/index、input 綁定 | 不通過 |
| QA-20260705-032 | 小程序品牌名稱 | 在工作台頁點開右上角小程序菜單後，菜單標題顯示「早点统计早下班」，與目前產品的團主/導遊領隊早下班定位及 DevTools 專案名稱不一致，使用者會以為進入了另一個小程序。 | 小程序菜單、導航標題、分享標題與正式產品名稱應一致；不得殘留舊產品或模板名稱。 | app.json、project.config.json、小程序帳號設定、分享配置 | 不通過 |
| QA-20260705-034 | 小程序預覽/上傳阻塞 | 此項驗證方式是 CLI `preview` / `upload`；目前強制規則禁止 CLI，因此 2026-07-06 未能重跑 CLI 驗證。舊紀錄顯示主包 2103KB 超過微信 2048KB 上限，需使用者明確授權 CLI 驗證或改由正式 GUI/工具清單接管。 | 使用者授權 CLI 驗證後，主包大小應低於 2048KB，且 `cli preview` / `cli upload` 能正常啟動；若不允許 CLI，此項應移出 GUI QA BUG 單，改由打包/工具檢查文件管理。 | `project.config.json`、資源白名單、`static` 大檔資源、`miniprogram_npm` | 不通過 |
| QA-20260705-035 | 人工 GUI 右側模擬被阻塞（二次驗證） | 此項依賴重新執行 `cli open / auto / preview`；目前強制規則禁止 CLI，因此 2026-07-06 未能重跑 CLI 二次驗證。需使用者明確授權 CLI，或決定此項移出 GUI QA BUG 單。 | 使用者授權 CLI 驗證後，`cli preview` 應成功進入模擬器；若不允許 CLI，此項不得由 GUI QA 假裝完成，應移到工具/打包驗證清單。 | 微信開發者工具 CLI、`cli preview`、`project.config.json`、`app.json` | 不通過 |
| QA-20260706-038 | 個人資料頁資料錯誤文案 | 2026-07-06 GUI 複測時，`pages/profile/index` 可打開，但頁面中央顯示「未知云端资料操作」，不是正式個人資料內容、正式空狀態或可理解的錯誤處理。 | 個人資料頁應顯示正式個人資料、正式空狀態或可操作的正式錯誤提示；不得顯示「未知云端资料操作」這類內部/不明錯誤文案。 | pages/profile/index、profile service、cloud data action mapping | 不通過 |
| QA-20260706-039 | 帳號資料頁返回 | 2026-07-06 GUI 複測時，從我的頁點「账号资料」可進入 `pages/my/info-edit/index`，但頁首返回按鈕點擊後仍停留在帳號資料頁，無法回到我的頁。 | 帳號資料頁頁首返回應回到我的頁，返回後 DevTools 页面路径、可見畫面與底部 tab selected 狀態必須一致。 | pages/my/info-edit/index、navigation utility、custom navbar | 不通過 |
| QA-20260706-041 | 團主資料頁返回 | 2026-07-06 GUI 複測時，從我的頁點「团主资料」可進入 `pages/tourGuides/index`，但頁首返回按鈕點擊後仍停留在團主資料頁，無法回到我的頁。 | 團主資料頁頁首返回應回到我的頁，返回後 DevTools 页面路径、可見畫面與底部 tab selected 狀態必須一致。 | pages/tourGuides/index、navigation utility、custom navbar | 不通過 |
| QA-20260706-042 | 團主資料頁資料錯誤文案 | 2026-07-06 GUI 複測時，`pages/tourGuides/index` 可打開，但頁面顯示「未知云端资料操作」，不是正式團主資料內容、正式空狀態或可理解的錯誤處理。 | 團主資料頁應顯示正式團主資料、正式空狀態或可操作的正式錯誤提示；不得顯示「未知云端资料操作」這類內部/不明錯誤文案。 | pages/tourGuides/index、tour guide service、cloud data action mapping | 不通過 |
