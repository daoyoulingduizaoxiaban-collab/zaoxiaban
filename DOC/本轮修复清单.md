# 本轮修复清单（用户 2026-07-28 报的 8 条）

> 这份是**防失忆骨干**：即使上下文压缩，也照这份继续做。每条做完 commit 一次并把「状态」改成 ✅。
> 用户要求：一次性全部处理完再回报，他再重新测。改数据层的云端动作必须同步改 `cloudfunctions/businessData`（地端云端双通铁律）。

## 进度总览
- [ ] #1 商品定价：改成填**总价**而非单价（买五送一→单价非整数无法填）
- [ ] #2 登录后显示「微信用户」，应显示该账号名称（改过多次仍坏）
- [ ] #3 新增团单「出团时间」点击无反应、不能输入
- [ ] #4 「收单截止」同样坏；并**扫全站所有页面**基本栏位操作，别等用户测到
- [ ] #5 团单表单删掉：取货集合、付款说明、联系人、联系电话 四个栏位
- [ ] #6 申请团主流程：申请→显示管理员微信 QR→加好友私聊→管理员审核通过→才能用；审有没有做完，没做完就补
- [ ] #7 微信开发工具里商品设置区块**滚不到**、设不了
- [ ] #8 架构重构：砍掉「商品库」，开团时**同步内嵌新增商品**；但预留日后拆回「两处分别设定」的架构

## 纠缠关系（顺序要对，否则白做）
- #8 重写「开团表单」（内嵌商品）↔ #5 删开团表单栏位 ↔ #1 商品定价表单 → **这三条协同做**。
- #3/#4 同一个坏选择器，很可能同根因。

---

## 逐条明细（做的时候填分析/方案/涉及文件）

### #1 商品定价改总价
- 现状：`priceSetting: [{minQuantity, unitPrice}]`，UI 填单价；买五送一 6 件 500 元→单价 83.33 非整数无法填。
- 方案：待定（填总价、存总价 or 反推单价；影响 product 表单 addPriceRule/tempPriceSetting、customerOrderService.calculateLine/getBestPriceRule、后端 products 价格处理）。
- 状态：⬜ 待处理

### #2 登录显示账号名
- 现状：登录后昵称显示「微信用户」（默认值），应显示账号实际名称。
- 方案：待查 authLogin/AuthService displayName 来源与为何不持久。
- 状态：⬜ 待处理

### #3 出团时间选择器无反应
- 现状：`sub-pages/groupOrder/add` 的 startAt 点击无反应、不能输入。
- 方案：待查 wxml 选择器绑定。
- 状态：⬜ 待处理

### #4 收单截止同样 + 全站栏位扫描
- 现状：endAt 同 #3；要求扫全站页面基本栏位操作。
- 状态：⬜ 待处理

### #5 删团单表单四栏位
- 现状：pickupNote/paymentNote/contactName/contactPhone。⚠后端 `validateGroupOrderPayload` **强制要求**这四个 → 删 UI 必须同步删前端校验+后端校验（地端+云端两份）。
- 状态：⬜ 待处理

### #6 申请团主 QR 审核流程
- 现状：待审——是否有「显示管理员微信 QR + 加好友 + 审核」完整流程。
- 状态：⬜ 待处理

### #7 商品设置滚不到
- 现状：开发工具里商品设置区块滚不到、设不了。疑似 scroll-view/布局高度问题。
- 状态：⬜ 待处理

### #8 内嵌商品重构（预留可拆回）
- 现状：现为「先去商品库新增商品→回团单关联」。要改成开团时直接塞商品。
- 方案：待定，须预留日后拆回两处架构。
- 状态：⬜ 待处理
