# 本轮修复清单（用户 2026-07-28 报的 8 条）

> 这份是**防失忆骨干**：即使上下文压缩，也照这份继续做。每条做完 commit 一次并把「状态」改成 ✅。
> 用户要求：一次性全部处理完再回报，他再重新测。改数据层的云端动作必须同步改 `cloudfunctions/businessData`（地端云端双通铁律）。

## 二轮稽核结论（2026-07-28 用户要求重核）
- **开团 create 三层校验已逐字确认全清**：前端 `buildFormError` / `GroupOrderService.validate` / 后端 `validateGroupOrderPayload` 都只剩 title+startAt+endAt(+日期格式)+商品，**无删除字段残留**。全仓 grep「联系人/取货」残留只命中**供应商表单**（与开团无关）。
- **「不能新建」真凶 = 日期**：startAt/endAt 仍必填但**原本无预设**，空着就被挡「请选择出团时间」。→ 已按 #1a **预设此时此刻**根治（就算不点 picker，日期已填，新建不再被挡）。
- **业务文件 `BUSINESS_LOGIC_PRINCIPLES.md`**：#5 删的 4 栏位经查**非业务规则**（文档无「团单必填取货/付款/联系人」条目）→ #5 无需改业务文件。**#8 隐藏商品库入口 → 必须改**（289/315 行）。本轮至今**未改过业务文件**（只查未写）。

## 进度总览
- [x] #1 商品定价：**已在开团内嵌表单实现**——填「起订量+总价(type=digit 支援小数)」，换算 `unitPrice=总价/起订量` 存(下单计价不变)，展示优先总价。标准商品库表单(已隐)同款改造列为可选后续。
- [~] #2 登录显示「微信用户」：**非显示 bug**——my 页 `name: displayName||'微信用户'` 读得对，是账号 displayName 没设过；微信已停用 getUserProfile，**app 无法自动取微信昵称**。已把 info-edit 名字输入改 `type="nickname"`（微信唯一合规方式，点一下带出昵称）；但仍需用户去「账号资料」设一次，登录不可能凭空显示微信名。**需用户确认这是否符合预期。**
- [x] #3 新增团单「出团时间」点击无反应 → 根因 picker `mode="datetime"` 是无效值；改用 tdesign date-time-picker（代码完成，待用户重测）
- [x] #1a 出团时间/收单截止**预设此时此刻**（新建）；编辑模式载入原值、picker 可改；顺带清掉 loadGroupOrder 里 4 个孤儿字段。**这条同时根治了 #2a「删栏位后不能新建」**（真凶是空日期被必填挡）
- [x] #4 「收单截止」同 #3 一并修好；全站 picker 扫描完成：**仅这两个 `mode=datetime` 坏、已修**，其余 picker（operationLogs/userReview 用 `mode=date`）全合法。完整「每页每栏位交互」审计需 IDE 跑（现不健康），随用户重测覆盖。
- [x] #5 删团单四栏位（前端表单+校验×2+后端校验+详情页显示）代码完成
- [x] #6 申请团主 QR 流程**已补完**：用户已把管理员微信 QR 放 `static/account/ivy.jpeg`（git 已追踪）。tourGuides/edit pending 态显示该 QR + 引导文案 + 点击放大；申请提交成功后当场重渲染成 pending 显示 QR（不跳走）。业务文件 81-91 段同步加了 QR 步骤说明。**待用户重测**。
- [x] #7 商品设置滚不到 → 根因：固定 footer(双按钮≈232rpx)高于容器 `padding-bottom:200rpx`、盖住底部商品区。改成 `calc(360rpx + safe-area)` 留足空间（代码完成，待重测）
- [x] #8 架构重构完成：**(A)开团页内嵌新增商品**——填名称+起订量+总价直接加入 selectedGoods（含 status=2 防下单页过滤），去掉跳商品库按钮（picker 代码保留可拆回）；**(B)商品库 tab 已隐藏**（custom-tab-bar 一行过滤）。业务文件 290/316 + 内嵌/定价说明同步。

> **每条强制收尾（用户要求）**：改动若涉及业务规则，必须同步改 `DOC/BUSINESS_LOGIC_PRINCIPLES.md`。#5 四栏位经查非业务规则、文档无需改；#8「商品库」是文档明写底部 NAV（289/315 行）必须同步改；#6 团主申请流程文档 26/81-91 有规则。
> **自动验证受阻**：过程中 IDE 运行时变不健康（automator `data()` 全页读空，含没改过的 my/index）→ 非代码问题，但自动化跑不了；#3/#4/#5 靠代码审查+tdesign 参数核对确认，**待用户 GUI 重编译后重测**。

## 纠缠关系（顺序要对，否则白做）
- #8 重写「开团表单」（内嵌商品）↔ #5 删开团表单栏位 ↔ #1 商品定价表单 → **这三条协同做**。
- #3/#4 同一个坏选择器，很可能同根因。

---

## 逐条明细（做的时候填分析/方案/涉及文件）

### #1 商品定价改总价
- 现状：`priceSetting: [{minQuantity, unitPrice}]`，UI 填单价；买五送一 6 件 500 元→单价 83.33 非整数无法填。
- 方案：待定（填总价、存总价 or 反推单价；影响 product 表单 addPriceRule/tempPriceSetting、customerOrderService.calculateLine/getBestPriceRule、后端 products 价格处理）。
- 状态：⬜ 待处理

### #2 登录显示账号名
- 现状：登录后昵称显示「微信用户」（默认值），应显示账号实际名称。
- 方案：待查 authLogin/AuthService displayName 来源与为何不持久。
- 状态：⬜ 待处理

### #3 出团时间选择器无反应
- 现状：`sub-pages/groupOrder/add` 的 startAt 点击无反应、不能输入。
- 方案：待查 wxml 选择器绑定。
- 状态：⬜ 待处理

### #4 收单截止同样 + 全站栏位扫描
- 现状：endAt 同 #3；要求扫全站页面基本栏位操作。
- 状态：⬜ 待处理

### #5 删团单表单四栏位
- 现状：pickupNote/paymentNote/contactName/contactPhone。⚠后端 `validateGroupOrderPayload` **强制要求**这四个 → 删 UI 必须同步删前端校验+后端校验（地端+云端两份）。
- 状态：⬜ 待处理

### #6 申请团主 QR 审核流程
- 现状：待审——是否有「显示管理员微信 QR + 加好友 + 审核」完整流程。
- 状态：⬜ 待处理

### #7 商品设置滚不到
- 现状：开发工具里商品设置区块滚不到、设不了。疑似 scroll-view/布局高度问题。
- 状态：⬜ 待处理

### #8 内嵌商品重构（预留可拆回）
- 现状：现为「先去商品库新增商品→回团单关联」（`onSelectGoods` 跳 product-picker 从库里挑）。
- **用户决策（2026-07-28）**：**开团页内嵌新增商品**（在开团表单里直接填 名称/价格/图，塞进 `selectedGoods`）；**商品库入口先「隐藏」**（不删代码/数据结构）——`pages/my/index.ts` 底部 NAV / 服务网格里把「商品库」入口隐藏；`products` 集合与商品管理页代码**保留**，日后可拆回。
- **同步改**：`DOC/BUSINESS_LOGIC_PRINCIPLES.md` 289/315 行「商品库=底部 NAV」的规则要改成「开团内嵌，商品库入口暂隐」。
- 执行要点：把 `onSelectGoods`（跳 picker）换成本页内嵌的「+新增商品」表单块（名称/价格 tier/图选填）；`selectedGoods` 直接 push；商品字段避开 `INTERNAL_PRODUCT_COPY_RE`。图片走 local 跳过、cloud 上传（同商品新增）。
- 状态：⬜ 待处理（蓝图已定，下轮执行）

### #1 定价（决策已定）
- **用户决策**：**填总价，且仍支援输入小数**。价格 tier 从「minQuantity + 单价」改成「minQuantity + 总价(允许小数)」。买五送一=6 件总价 500 直接填。
- 待实现时定清：客户下单 `calculateLine`/`getBestPriceRule` 的换算——tier 存总价后，客户点某数量时单价=总价/该 tier 件数？还是只在整档数量时用总价？**实现时确认这条映射**。影响：商品表单 `addPriceRule`/`tempPriceSetting`（放开整数限制、字段改总价）、`customerOrderService` 计价、后端 products 价格字段、开团页 price-badge 展示文案（现为「N件起:¥单价」）。
- 与 #8 一起做（#8 的内嵌商品表单就用这套新定价）。
- 状态：⬜ 待处理（决策已定）

### #6 管理员 QR（决策已定）
- **用户决策**：QR 图**放进项目资源写死**。→ 需用户把管理员微信 QR 图片文件放到项目里（如 `assets/admin-wechat-qr.png`），代码写死引用。
- 执行：申请团主提交后（`guideApplication.status=pending` / tourGuides/edit 申请模式提交后 / my 页 pending 态），展示该 QR + 文案「扫码加系统管理员微信好友，私聊沟通，等待通过后即可使用团主功能」。
- **阻塞**：需用户提供 QR 图片文件（我没有）。UI 可先接一个固定 assets 路径，图由用户放入。
- 状态：⬜ 待处理（决策已定，等图片文件）
