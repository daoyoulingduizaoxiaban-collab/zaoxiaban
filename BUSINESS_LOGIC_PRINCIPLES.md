# BUSINESS_LOGIC_PRINCIPLES

## 文件用途
本文件记录项目的核心业务逻辑原则，供 AGENT、QA 和产品决策对齐使用。

它不是 BUG 清单、验收结果、开发进度或技术实现计划。具体待办看 `MVP_COMPLETION_CHECKLIST.md`，角色入口细节看 `ROLE_FEATURE_ACCESS_MATRIX.md`，资料字段与权限边界看 `DATA_MODEL_AND_PERMISSIONS.md`。

## 1. 产品主线
本产品是给中国导游/领队使用的微信小程序。MVP 的核心闭环是：

1. 用户通过微信登录，取得正式 OpenID。
2. owner/admin 审核用户身份与角色。
3. 供应商维护商品或服务。
4. 导游/领队选择商品并开团。
5. 客户从团单入口下单并声明付款。
6. 导游/领队确认收款并查看订单状态。

任何功能若不能支持这条闭环，应被视为非核心能力；不得为了非核心能力牺牲登录、权限、团单、商品、订单和收款闭环。

## 2. 身份与角色
微信 OpenID 是正式身份锚点。正式用户的业务权限必须来自云端 `users` profile 或等价正式资料，不得来自前端参数、本地 storage、QA override、分享链接或 mock 资料。

用户状态至少分为：

- `pending_review`：已登录但等待审核。
- `approved`：已审核通过。
- `rejected`：审核被拒。
- `disabled`：账号停用。

正式业务功能只对 `approved` 且具备对应角色或场景权限的用户开放。未登录、待审核、被拒或停用用户不能看到完整业务入口，也不能绕过前端直接调用后端写入。

角色能力可以是多重的。同一个 OpenID 可以同时是导游/领队，也可以在别人分享的团单里作为客户下单。系统不能只用单一 `role` 粗暴判断所有场景，必须根据当前入口和业务场景计算 `effectiveRole` 或等价有效身份。

## 3. Owner 与运营验收
第一位 owner 必须由受控 allowlist 或等价机制产生，不能由前端自选。owner 是最高业务管理者，可以审核用户、管理关键资料和执行上线前验收。

为了让同一个微信账号测试不同角色，系统可以提供 owner 专用的「运营验收模式」。该模式只能改变预览视角，不得修改 owner 的真实角色。真实身份与预览身份必须分离：

- `realProfile`：当前 OpenID 的真实云端身份。
- `effectiveProfile`：当前页面和流程使用的预览身份。

只有真实 owner 且状态为 `approved` 时，才可以启用角色预览。普通用户不得通过参数伪造 owner、admin、guide、customer 或 provider。

## 4. Customer 下单规则
customer 规则分两种入口处理。

直接打开小程序、普通扫码、首页、搜索或非团单分享路径进入的用户，首次登录后必须进入 `pending_review`，不得自动变成完整 customer。

从合法团单分享链接、团单码或带有效 share token 的入口进入的用户，可以获得受限客户下单能力。受限客户只允许：

- 查看该分享团单。
- 查看该团单可下单商品。
- 提交自己的订单。
- 声明自己的付款。
- 查看自己的订单和付款状态。

受限客户不得进入完整业务 tab、商品库管理、导游工作台、供应商后台、资料中心、用户审核或其他客户资料。

如果未来需要完整 customer 会员能力，完整 customer 仍必须经过 owner/admin 审核。受限下单身份不等于完整 customer 角色。

## 5. Provider 供应商规则
provider 是 MVP 正式角色，不是后续占位角色。

供应商必须能完成以下正式流程：

- 申请成为 provider。
- 由 owner/admin 审核通过、拒绝或停用。
- 维护自己的供应商资料。
- 新增、编辑、上下架自己的商品或服务。
- 让导游/领队在开团或本团商品流程中选用可用商品。
- 让客户在下单时看到必要供应商资讯。

provider 只能管理自己的资料和商品。provider 不得看到导游团单管理、客户资料中心、用户审核、owner/admin 功能、其他供应商资料或其他供应商商品管理。

provider 被停用后，不得新增或修改商品；其既有交易资料必须保留历史状态，不得直接删除造成订单追溯断裂。

## 6. 导游/领队与团单
guide 是团单经营者。导游/领队可以建立团单、维护本团商品、查看自己团单下的客户订单，并确认收款。

团单必须有清楚归属。导游只能管理自己创建或被授权管理的团单。客户只能通过分享或自己的订单关联读取指定团单的可下单信息。供应商默认不能管理团单。

团单商品应保存商品快照，避免供应商商品后续变更影响历史订单无法追溯。商品下架或供应商停用后，新团单不得继续选用；既有团单和订单应显示正式状态，而不是删除历史。

## 7. 商品与资料归属
商品可以来自导游/领队、供应商或 owner/admin 管理。每个商品必须有明确归属、状态、价格、图片和说明。

资料归属必须清楚：

- 导游身份建立的商品和团单归属于导游 principal。
- 供应商身份建立的商品归属于 provider principal。
- 客户身份建立的订单归属于 customer principal。
- owner/admin 可以按权限查看和管理全局资料，但操作必须可追溯。

同一 OpenID 在不同场景下的资料不能混用。一个人既是导游又是客户时，不能因为 OpenID 相同就看到或修改不属于当前场景的资料。

## 8. 订单与收款
客户订单记录客户在指定团单内的购买内容、金额、付款状态和历史。

付款声明由客户发起，导游/领队确认。MVP 不要求付款凭证图片必填；客户可用付款方式、付款金额和备注提交声明，图片凭证是选填补充证据。

付款状态必须可追溯，至少区分：

- 未付款。
- 客户已声明付款。
- 导游/领队已确认收款。
- 已取消。

导游只能确认自己团单下的订单收款。客户只能查看和处理自己的订单。provider 默认不能查看客户个人付款资料。

## 9. 正式资料层原则
MVP 正式资料层采用微信云数据库 + 云函数。页面不得直接读写正式数据库、云函数、storage 或 `mock/qaSeed.ts`；页面只调用 service/repository。

云函数必须执行最终权限判断。前端隐藏入口只是体验优化，不能替代后端权限。

mock、local、QA seed 只用于开发和验收辅助，不能包装成正式数据。正式模式下不得向普通用户暴露 mock、Seed、QA、debug、MVP、未完成、待串接等内部字样。

## 10. 上线判断原则
上线判断以真实业务闭环是否可完成为准，不以 commit 数量、文件打勾或静态检查数量为准。

可以视为 MVP 可上线的最低条件是：

- owner/admin 能审核用户。
- provider 能维护供应商资料与商品。
- guide 能开团并选择商品。
- customer 能通过合法团单入口下单。
- customer 能声明付款，凭证选填。
- guide 能确认收款。
- 资料重开后仍存在。
- 无权角色在前端和后端都不能越权。
- 核心流程经过 fresh DevTools 或真机验证。

没有 fresh GUI/真机或等价可接受操作证据时，不能把 GUI、workflow、图片、付款、分享、权限等项目宣告完成。
