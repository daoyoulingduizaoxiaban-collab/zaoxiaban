# DATA_LAYER_DECISION

## 目的
本文件只做 Phase 1 正式资料层方案比较与建议，不代表已经接入正式数据库、云函数或后端 API。

当前项目仍是 QA 展示模式：`config.js` 为 `isMock: true`，主流程资料来自 `mock/qaSeed.ts` 与本地页面状态。`mock/qaSeed.ts` 后续只能保留为测试资料来源，不可作为真人操作的唯一资料来源。

## 2026-07-02 使用者确认
本轮使用者已确认 MVP 正式资料层方向：优先采用「微信云开发数据库 + 云函数」。页面仍不得直接散落调用云数据库，必须通过资料存取层 / repository / service 边界接入，并保留未来切换明确后端 API 的可能。

## 方案 A：微信云开发数据库

### 成本
- 初期成本低，MVP 可使用微信生态内置数据库、云函数、云存储与云调用能力。
- 对单一微信小程序产品较友好，早期不需要额外维护独立服务器。
- 成本会随云函数调用量、数据库容量、云存储流量增长，需要后续设置预算与告警。

### 开发速度
- 与微信小程序集成速度最快，尤其是 OpenID、云函数、云数据库集合读写。
- 适合本项目先补齐导游/领队核心闭环：团单、商品、本团商品、客户订单、收款状态。
- 若直接在页面散落调用云数据库，后续会难迁移；必须先建立资料存取层接口。

### 登录 / OpenID 整合
- OpenID 获取与微信身份绑定最直接。
- 可用 OpenID 作为 `users.openId` 的唯一身份来源，再由业务角色决定 guide/customer/provider/admin 可见范围。
- Phase 2 才能正式实现登录与 user profile 初始化；本轮不实现。

### 资料权限
- 云数据库安全规则可限制用户只访问自己或被授权的数据。
- 复杂权限建议放到云函数执行，避免客户端直接构造越权查询。
- 团单、商品、订单、付款状态变更应由云函数按角色校验后写入。

### 部署维运
- 维运门槛低，适合小团队快速交付。
- 需要管理云函数版本、数据库索引、安全规则、备份与数据导出。
- 与微信生态耦合较强，非微信端复用能力有限。

### 未来扩充风险
- 若未来需要多端 App、Web 后台、复杂报表、供应商系统、外部支付/ERP 串接，云开发会逐渐受限。
- 数据模型和权限若一开始写死在页面或云数据库规则里，迁移到独立后端成本会高。
- 建议从第一天建立 repository/service 层，页面只依赖业务接口，不直接依赖云数据库 SDK。

## 方案 B：明确后端 API

### 成本
- 初期成本较高，需要后端服务、数据库、部署环境、域名、HTTPS、监控与备份。
- 若团队已有后端基础设施或未来确定多端扩展，长期成本可控。

### 开发速度
- MVP 起步较慢，需要先设计 API、鉴权、部署、数据库迁移与运行环境。
- 对复杂业务规则、审计日志、报表、跨端复用更稳定。

### 登录 / OpenID 整合
- 小程序端仍需获取微信登录 code，再由后端换取 OpenID/session。
- 后端可统一处理 user profile、角色、授权、token/session。
- 对多端身份合并更灵活，但实现量高于云开发。

### 资料权限
- 权限边界集中在服务端，更适合 owner/admin、guide、customer、provider 多角色长期演进。
- 可做细粒度审计、幂等操作、支付状态历史、软删除与后台管理。
- API 必须从一开始避免客户端传什么就信什么，所有 owner/guide/customer/provider/admin 权限都由服务端判定。

### 部署维运
- 需要维护服务健康、日志、监控、备份、数据库迁移、扩容与安全更新。
- 可更容易接 CI/CD、灰度发布、回滚与独立管理后台。

### 未来扩充风险
- 架构弹性较好，适合供应商后台、Web 管理端、跨平台客户下单、复杂支付与财务对账。
- 初期若过度设计，会拖慢 MVP，增加导游核心闭环上线风险。

## 建议
建议 MVP 第一阶段采用「微信云开发数据库 + 云函数」作为正式资料层，但必须同时建立清晰的资料存取层接口：
- 页面不得直接散落调用 `mock/qaSeed.ts`、storage、云数据库或未来 API。
- 所有 create/update/delete 由 repository/service 返回成功、失败、错误信息，UI 负责 loading、成功、失败状态。
- 关键写入走云函数校验 OpenID 与角色权限，不让客户端直接越权写集合。
- `mock/qaSeed.ts` 只作为测试资料与本地 QA 展示来源，后续以同一 repository 接口切换 mock/cloud。

选择理由：
- 本项目是单一微信小程序，当前最急迫是从 QA 展示推进到真人可用导游 MVP。
- 微信云开发对 OpenID 与小程序环境集成最快，可降低 Phase 2/3 的初期交付成本。
- 通过资料存取层隔离云开发依赖，可保留未来迁移到明确后端 API 的余地。

需要使用者确认后才能进入正式实现：
- 是否同意 MVP 正式资料层先用微信云开发。
- 是否需要从第一版就预留 Web 后台或供应商后台。
- 是否已有既定后端、数据库、云账号或公司部署标准。

## 本轮未做
- 未接入微信登录。
- 未创建云数据库集合。
- 未创建云函数。
- 未新增后端 API。
- 未实现正式保存。
